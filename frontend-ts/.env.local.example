# Frontend environment variables (OPTIONAL)
# The frontend defaults to http://localhost:8000 - no configuration needed for local development!
# Only create .env.local if you need to override the default backend URL.

# For production deployment or custom backend URL:
# NEXT_PUBLIC_STORYCRAFT_API_BASE=https://your-backend-api.example.com

# For local development (default - no need to set):
# NEXT_PUBLIC_STORYCRAFT_API_BASE=http://localhost:8000
